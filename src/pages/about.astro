---
import BaseLayout from '../layouts/BaseLayout.astro';
import Footer from '../components/Footer.astro';
import SocialLinks from '../components/SocialLinks.astro';
import { getSiteSettings } from '../lib/queries';
import { youtubeEmbedUrl } from '../lib/video';

const settings = await getSiteSettings();
const socialSettings = { ...settings, contact_email: settings?.contact_email || 'akpotohwoo@gmail.com' };

const aboutVideo = settings?.about_video_url ? youtubeEmbedUrl(settings.about_video_url) : null;

const aboutIntro = settings?.about_intro || "I'm Chukwuneku Akpotohwo. Most people call me Neku. I'm a software engineer focused on AI, studying at Babcock University, and most of what I believe fits in one line: intelligence is built, not found.";
const aboutGdg = settings?.about_gdg || "Since 2024 I built GDG on Campus Babcock into one of Nigeria's premier tech communities and the largest developer community on campus. Summits, a publication, and a room where a thousand-something people teach each other things nobody assigned them. I'm handing it to the next set of hands now, which was always the point of building something that outlasts you.";
const aboutInterests = settings?.about_interests || "Off the clock I read cognitive psychology and take ideas apart until they stop reducing. I care about what grows minds, mine included, and what that looks like at the scale of a person, a community, a continent.";
---

<BaseLayout
  title="About | Neku Akpotohwo"
  description="Who Chukwuneku 'Neku' Akpotohwo is: AI-focused software engineer, organizer of GDG on Campus Babcock, writer."
  skyMode="receded"
>
  <div class="about">
    <header class="about__header">
      <h1>About</h1>
    </header>

    <section class="about__section">
      {settings?.portrait_url && (
        <img
          class="about__portrait"
          src={`${settings.portrait_url}?w=640&auto=format`}
          alt="Chukwuneku 'Neku' Akpotohwo"
          width="200"
          height="240"
          loading="lazy"
        />
      )}
      <p>{aboutIntro}</p>
      <p>{aboutGdg}</p>
    </section>

    {aboutVideo && (
      <section class="about__video">
        <div class="video-embed">
          <iframe
            src={aboutVideo}
            title="Neku on GDG Babcock"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            loading="lazy"
          ></iframe>
        </div>
      </section>
    )}

    <section class="about__section">
      <p>{aboutInterests}</p>
    </section>

    <div class="about__reach">
      <SocialLinks settings={socialSettings} align="start" />
    </div>
  </div>

  <Footer />
</BaseLayout>

<style>
  .about {
    max-width: 65ch;
    margin: 0 auto;
    padding: calc(80px + var(--space-2xl)) var(--space-xl) var(--space-3xl);
  }

  .about__header {
    margin-bottom: var(--space-2xl);
  }

  .about__header h1 {
    font-size: var(--text-4xl);
  }

  .about__section {
    margin-bottom: var(--space-2xl);
  }

  .about__portrait {
    float: right;
    width: 200px;
    height: 240px;
    object-fit: cover;
    border-radius: 12px;
    margin: 0 0 var(--space-lg) var(--space-lg);
    border: 1px solid rgba(236, 227, 214, 0.08);
  }

  @media (max-width: 560px) {
    .about__portrait {
      float: none;
      display: block;
      margin: 0 0 var(--space-lg) 0;
    }
  }

  .about__section h2 {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 400;
    color: var(--paper);
    margin-bottom: var(--space-lg);
  }

  .about__section p {
    color: var(--muted);
    font-size: var(--text-base);
    line-height: 1.8;
    margin-bottom: var(--space-lg);
  }

  .about__section a {
    color: var(--lamp);
    text-decoration-color: rgba(224, 164, 88, 0.3);
  }

  .about__section a:hover {
    text-decoration-color: var(--lamp);
  }

  .about__video {
    margin-bottom: var(--space-2xl);
  }

  .about__reach {
    margin-top: var(--space-2xl);
  }

  .video-embed {
    position: relative;
    padding-bottom: 56.25%;
    border-radius: 12px;
    overflow: hidden;
    background: var(--raised);
    margin-top: var(--space-lg);
  }

  .video-embed iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  @media (max-width: 480px) {
    .about {
      padding-left: var(--space-md);
      padding-right: var(--space-md);
    }
  }
</style>

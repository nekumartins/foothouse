# Commit straight to main as nekumartins, unsigned, no co-author trailers.

Owner's explicit instruction (2026-07-02), overriding the default work-on-a-feature-branch flow: develop and push on `main`; author identity `nekumartins <akpotohwoo@gmail.com>`; commits must not be GPG-signed and must not carry Co-Authored-By trailers. It mattered because the harness default was a `claude/*` branch with a co-author trailer, and the owner rejected that at plan approval.
